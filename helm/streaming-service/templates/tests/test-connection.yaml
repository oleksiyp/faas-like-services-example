apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streaming-service.fullname" . }}-test-connection"
  labels:
    {{- include "streaming-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streaming-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
