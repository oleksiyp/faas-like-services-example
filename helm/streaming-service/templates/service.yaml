apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming-service.fullname" . }}
  labels:
    {{- include "streaming-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streaming-service.selectorLabels" . | nindent 4 }}
